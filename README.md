# fpvmavros

Проект дрона с самонаведением на основе Foxeer F722 V4 и RaspberryPi 4.

## Шаги по началу работы
### Подготовка среды на raspberry pi 4

```
# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker $USER
```
